all this files is the source code of compiled files in the root folder compiled/
is in python compiled with auto-py-to-exe, simple, executed by aplication to hear the words and send to node application
if you want to execute this files is necessary to download the respective library
how to execute:
python detect.py
python hear.py "bot name : path"
python execute.py "json path"
python execute_hear "command in voice to execute the json:json path"

